// Program to check whether a year is a leap year or not .Using switch statement
// LOGIC - IF year divisible by 4 then -
//                    IF divisible by 100 -
//                            IF divisible by 400 then leap year
//                            ELSE not leap year
//                    ELSE leap year
//         ELSE not leap year
#include<stdio.h>
int main()
{
    int year;
    printf("Enter a year:");
    scanf("%d",&year);

    

    return 0;
}