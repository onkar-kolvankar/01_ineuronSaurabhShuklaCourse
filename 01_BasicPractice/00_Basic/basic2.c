#include <stdio.h>
int main()
{
    int a;
    char b;
    double c;
    a= sizeof(b);

    //printf("The size of Integer Constant on memory is = %d",a);
    //printf("The size of Character Constant on memory is = %d",a);
    printf("The size of Variable on memory is = %d",a);


    return 0;
}

